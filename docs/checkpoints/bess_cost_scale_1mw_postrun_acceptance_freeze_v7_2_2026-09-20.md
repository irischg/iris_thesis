# PNNL 1 MW BESS Cost-Scale Sensitivity — Independent Post-Run Acceptance Freeze (v7.2)

**Artifact class:** additive, independent, repository-level acceptance record.
**Date:** 2026-09-20
**Run under acceptance:** `20260919T151258567077Z_0541e6897a`
**Lineage:** `SENS-COST1MW-R10`
**Produced by:** the V7.2 production version-lineage / rerun-difference freeze R2 recovery gate
(`results/provenance/v7_2_production_version_lineage_freeze_candidate_r2/`).

---

## 1. Immutability statement — read this first

The original completion manifest of the run,

```
results/sensitivity/bess_cost_scale_1mw/runs/20260919T151258567077Z_0541e6897a/completion_manifest.json
```

**remains byte-identical and its status remains exactly**

```
CANDIDATE_PASS_PENDING_INDEPENDENT_POST_RUN_AUDIT
```

That manifest was **not** modified, re-signed, re-verdicted or regenerated by this gate, and it must
never be. It is the runner's own self-report, and a runner may not accept its own output. Its field
`completion_is_not_self_acceptance: true` is correct and is preserved.

**This file is a separate, additive artifact.** It records an *independent repository-level
acceptance* derived only from pre-existing immutable evidence. It does not replace, amend or
supersede the original completion manifest.

---

## 2. Verification performed

Read-only re-derivation from local evidence only. **Zero optimize calls, zero model constructions,
zero writes to the run directory.** No artifact under the run directory was modified.

| Requirement | Evidence | Result |
|---|---|---|
| Exactly 1 production invocation | one run directory; one `run_manifest.json`; `run_id` agrees across run manifest and completion manifest | **PASS** |
| Exactly 6 sensitivity optimize calls | `solve_count = 6`, independently corroborated by exactly one Gurobi `Optimize a model with` header in each of the six per-case `gurobi.log` files (6 total) | **PASS** |
| 0 comparator solves | `mainline_comparator_solves = 0`; comparator metrics read from frozen 10 MW result artifacts | **PASS** |
| 0 retries | `retry: false` in the solver contract; no restart/retry marker in any per-case log | **PASS** |
| All six OPTIMAL | every case `status = OPTIMAL`, `mode = binary` | **PASS** |
| All exact gaps ≤ 1e-6 | max realized gap `7.78553119379967e-07` (case `a0.80_b08`); all six below 1e-6 | **PASS** |
| Mandatory post-solve gates PASS | EOB 14/14 gates PASS; each of the five representative cases 22/22 gates PASS | **PASS** |
| Rainflow PASS | all six cases `RAINFLOW_VALIDATION_PASS` with every rainflow sub-gate PASS | **PASS** |
| Artifact integrity | all **124** artifact-registry entries re-hashed and byte-size checked against the registry; **0** failures | **PASS** |
| 1 MW package identity | every case carries package object SHA-256 `38432cb791dccc95c4c178def34bb0fb97651802953338adb0397cb883fafac7`, `package_id = pnnl_v2024_lfp_2023_point_1mw_4to10h`; per-case degradation cost audit reconciles CAPEX/FOM/degradation against that package with max residual `1.67e-10` NTD | **PASS** |
| One-factor fairness | pre-solve gate records `winter_pv_treatment = MAINLINE_CONSERVATIVE_ZERO`, `alternative_input_used = false`, canonical input SHA-256 `9142b8b6…518e`, core r10 `9d828321…3da8` | **PASS** |
| Terminal-state exclusivity | `completion_manifest.json` present; `failure_manifest.json` absent | **PASS** |
| Protected rehash | corrected core, Framework, Registry, canonical annual input, economic interface, tariff, settlement interface, settlement matrix, both PNNL package files re-hashed — all byte-identical | **PASS** |
| Repository boundary | branch `thesis-v7`; HEAD = `origin/thesis-v7` = `b03721275c73b05d45517bdf84c1e0bd03833376`; staged = 0; tracked-modified set unchanged (the accepted eight) | **PASS** |

The implementation itself was independently audited before execution; that audit is recorded at
`docs/checkpoints/bess_cost_scale_1mw_implementation_candidate_v7_2_2026-09-19.md`
(SHA-256 `31d23e45d4d1bb344f81ad0aa244fdbcbb46339ba98c16a33d2c5e0603e37890`).

---

## 3. Acceptance

On the evidence above, independently re-derived rather than read from the runner's own verdict:

```
SENS-COST1MW-R10  ->  ACCEPTED_SENSITIVITY
```

**Scope and boundary.** This acceptance covers the EOB plus five representative cases only. It is
**not** a complete alternative 81-point response surface. The 1 MW label is an **economic
source-case label, not a P_B bound** (Gate 1, CLOSED). `SENS-COST1MW-R10` is a **sensitivity
branch**: it does **not** supersede `MAINLINE-CORRECTED-R10`, and the 10 MW package remains the
mainline.

**What this artifact does not do.** It does not alter the run, does not alter the original
completion manifest, does not confer authority on any future run, and does not authorize the SOC
sensitivity, the corrected final-81 surface or the A2 robustness branch.

---

## 4. Non-mutation record

- Optimize calls: **0**
- Model constructions: **0**
- Files under the run directory modified: **0**
- Original completion manifest status: **unchanged**, `CANDIDATE_PASS_PENDING_INDEPENDENT_POST_RUN_AUDIT`
- Git commit / push / tag / reset / clean / stash / checkout: **0**
